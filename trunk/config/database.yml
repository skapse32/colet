# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: mysql2
  encoding: utf8 
  host: murta.pt
  database: colet
  pool: 10 
  username: colet
  password: 12colet34pass 
  reconnect: true

# Não esquecer definir o modo de producção correctamente e activá-lo antes do deployment
production:
  adapter: sqlite3
  database: db/production.sqlite3
  pool: 5
  timeout: 5000
